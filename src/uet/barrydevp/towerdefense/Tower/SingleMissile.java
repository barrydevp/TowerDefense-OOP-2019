package uet.barrydevp.towerdefense.Tower;import javafx.geometry.Point2D;import javafx.scene.Node;import javafx.scene.image.ImageView;import uet.barrydevp.towerdefense.Bullet.BaseBullet;import uet.barrydevp.towerdefense.Bullet.BigRocket;import uet.barrydevp.towerdefense.Game.GameManager;import uet.barrydevp.towerdefense.Game.GameSpriteManager;import uet.barrydevp.towerdefense.Monster.BaseMonster;import uet.barrydevp.towerdefense.Sprite.BaseSprite;import uet.barrydevp.towerdefense.Sprite.TowerSprite;import java.util.ArrayList;public class SingleMissile extends BaseTower {    @Override    public boolean init() {        setTowerName(TowerName.SingleMissile);        return true;    }    public static SingleMissile creatTower(Point2D location) {        SingleMissile tower = new SingleMissile();        if(tower.init()) {//            ArrayList<Node> children = new ArrayList<>();//            ImageView fireEffect = new ImageView(GameSpriteManager.getInstance().fireSingleMissileEffectImage);//            fireEffect.setVisible(false);//            fireEffect.setFitHeight(10);//            fireEffect.setFitWidth(10);//            fireEffect.relocate(30, 30);//            children.add(fireEffect);            tower.setBaseSprite(                    new TowerSprite(                            GameSpriteManager.getInstance().singleMissileImage,                            null,                            location, 90)            );            tower.setBuildMoney(250);            tower.setUpdateMoney(200);            tower.setForce(500);            tower.setLevel(1);            tower.setScope(150);            tower.setNextScope(250);            tower.setRate(1.5);            tower.addToLayer();        }        return tower;    }    public BaseBullet createBullet(BaseMonster targetMonster, Point2D center) {        BigRocket bullet = BigRocket.createBullet(targetMonster, center);        return bullet;    }}