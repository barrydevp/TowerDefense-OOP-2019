package uet.barrydevp.towerdefense.Game;import javafx.application.Application;import javafx.scene.Scene;import javafx.scene.transform.Scale;import javafx.stage.Stage;import uet.barrydevp.towerdefense.Utils.SaveAndLoadGame;public class Game extends Application {    @Override    public void start(Stage primaryStage) {        GameMenu instanceGameMenu = GameMenu.getInstance();        instanceGameMenu.startMainMenuGame();        Scene scene = instanceGameMenu.getScene();        primaryStage.setScene(scene);        // fullscreen        primaryStage.setFullScreen( GameSetting.STAGE_FULLSCREEN);        primaryStage.setFullScreenExitHint("");        // scale by factor of 2 (in settings we have half-hd) to get proper dimensions in fullscreen (full-hd)        if( primaryStage.isFullScreen()) {            Scale scale = new Scale( GameSetting.STAGE_FULLSCREEN_SCALE, GameSetting.STAGE_FULLSCREEN_SCALE);            scale.setPivotX(0);            scale.setPivotY(0);            scene.getRoot().getTransforms().setAll(scale);        }        instanceGameMenu.setPrimaryStage(primaryStage);        primaryStage.setWidth(GameSetting.SCENE_WIDTH * GameSetting.SCALE_WIDTH);        primaryStage.setHeight(GameSetting.SCENE_HEIGHT * GameSetting.SCALE_HEIGHT);        primaryStage.show();    }    public static void main(String[] args) {        launch(args);    }}