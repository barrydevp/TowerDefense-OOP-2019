package uet.barrydevp.towerdefense;import javafx.scene.image.Image;import javafx.scene.image.ImageView;import javafx.scene.layout.Pane;import javafx.geometry.Point2D;public class Sprite {    private Image image;    private ImageView imageView;    private Pane layer;    private Point2D centerLocation;    private final double originalR;    private double r;    private double width;    private double height;    public Sprite(Pane layer, Image image, Point2D centerLocation, double r) {//        System.out.println(centerLocation);        this.layer = layer;        this.image = image;        this.centerLocation = centerLocation;        this.originalR = r;        this.r = r;        this.width = image.getWidth(); // imageView.getBoundsInParent().getWidth();        this.height = image.getHeight(); // imageView.getBoundsInParent().getHeight();        this.imageView = new ImageView(this.image);        this.imageView.relocate(centerLocation.getX() - width / 2, centerLocation.getY() + height / 2);//        this.imageView.relocate(0, 0);        this.imageView.setRotate(r);        addToLayer();    }    public void addToLayer() {        layer.getChildren().add(imageView);    }    public void removeFromLayer() {        layer.getChildren().remove(imageView);    }    public Pane getLayer() {        return layer;    }    public void setLayer(Pane layer) {        this.layer = layer;    }    public Point2D getCenterLocation() {        return centerLocation;    }    public void setCenterLocation(Point2D centerLocation) {        this.centerLocation = centerLocation;    }    public double getR() {        return r;    }    public void setR(double r) {        this.r = r + originalR;    }    public ImageView getImageView() {        return imageView;    }    public void updateUI() {//        System.out.println("Update UI: " + drawLocation.getX());//        imageView.relocate(100, 0);//        imageView.setX(500);        imageView.relocate(centerLocation.getX() - width / 2, centerLocation.getY() + height / 2);        imageView.setRotate(r);//        System.out.println("X:" + imageView.getX() );    }    public void update(Point2D centerLocation, double dr) {        this.r = this.originalR + dr;        this.centerLocation = centerLocation;        this.updateUI();    }    public double getWidth() {        return width;    }    public double getHeight() {        return height;    }    public void removeSprite() {        removeFromLayer();    }}