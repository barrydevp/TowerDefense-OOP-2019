package uet.barrydevp.towerdefense.Sprite;import javafx.geometry.Point2D;import javafx.scene.image.Image;import javafx.scene.image.ImageView;public class EffectSprite extends BaseSprite {    private ImageView imageView;    private Image image;    public EffectSprite(Image image, Point2D position, double r) {        this.centerLocation = position;        this.image = image;        this.width = image.getWidth();        this.height = image.getHeight();        this.imageView = new ImageView(image);        this.originalR = r;        this.r = r;        getChildren().add(imageView);//        relocate(centerLocation.getX() - 500, centerLocation.getY() - 500);//        setTranslateX(centerLocation.getX() - 32);//        setTranslateY(centerLocation.getY() - 32);        setTranslateX(centerLocation.getX() - image.getWidth() / 2);        setTranslateY(centerLocation.getY() - image.getHeight() / 2);        setRotate(r);        setVisible(false);    }    public void addChildEffect(Image image, Point2D offset, double r) {        ImageView tmp = new ImageView(image);        tmp.relocate(offset.getX(), offset.getY());        tmp.setRotate(r);        getChildren().add(tmp);        getChildren().get(0).relocate(-offset.getX(), -offset.getY());    }}